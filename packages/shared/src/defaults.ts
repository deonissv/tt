import { Vector3 } from '@babylonjs/core/Maths/math.vector';

export const DEFAULT_MASS = 1;
export const DEFAULT_SCALE = Vector3.FromArray([1, 1, 1]);
export const DEFAULT_ROTATION = Vector3.FromArray([0, 0, 0]);
export const DEFAULT_POSITION = Vector3.FromArray([0, 0, 0]);
